#include "main.h"
/**
 * add - adds to integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: return the result of add operation
 */

int add(int a, int b)
{
	return (a + b);
}
